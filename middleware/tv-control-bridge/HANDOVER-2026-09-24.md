# HANDOVER — Lanjutan Besok (2026-09-24)

Pekerjaan 2026-09-23 sudah disimpan. Baca dokumen ini dulu sebelum menyentuh apa pun.

## 1. Yang sudah selesai hari ini

  A. Overlay di layar TV (jalur C) — TERBUKTI
     APK com.happysong.tvnotify terpasang di VIP-4 (izin overlay aktif), proyek di
     C:\karaoke-tv-bridge\tv-notify-overlay. Uji T1-T7 lolos. Bukti: HASIL-UJI-OVERLAY-TV.md.

  B. Bridge memegang jadwal peringatan — TERBUKTI
     Peringatan T-15/T-5 di layar TV + peniduran TV di T-0 berikut tenggang, retry, catatan kejadian
     (logs/tv-events.jsonl), jadwal tahan restart (data/jadwal-ruangan.json), token bersama hidup.
     Bridge: C:\karaoke-tv-bridge (repo git sendiri, commit terakhir 3fdeb72).

  C. Integrasi ke POS — TERBUKTI LEWAT API POS
     - server/src/services/tvBridgeService.js (baru): klien bridge + jadwal + penyapu + audit.
     - routes/api.js: setelah aksi sesi berhasil, jadwal bridge disesuaikan dari tabel rooms.
     - server.js: penyapu saat server hidup, status bridge di /sync/status, /tv-bridge-config.js.
     - js/config.js + js/app.js + index.html: alamat bridge 192.168.1.3:3030, token dari server
       (tidak disimpan di repo karena repo PUBLIK), batas waktu 8 detik.
     Branch: feat/tv-bridge-schedule-integration (commit 2c7ef13, a7175d0) di atas 6ee074a.

  D. Database bersih dari data uji
     Dua transaksi uji (TRX-1790157321548 Rp32.000 dan TRX-1790155003199 Rp28.000) beserta
     2 sesi, 2 segmen sesi, dan 4 baris outbox sudah DIHAPUS pada 2026-09-23.
     Pembanding: 519 transaksi lain identik sebelum-sesudah (sidik md5 sama), total nilai
     turun tepat 60.000. Sisa rujukan ke id uji: 0. Cadangan baris yang dihapus ada di
     cadangan-20260923\hapus-transaksi-uji-20260923.json (hapus berkas ini bila tidak diperlukan).

## 2. Yang MENUNGGU dan perlu keputusan/tindakan

  1. Push + Pull Request belum jalan: branch masih lokal. Git Credential Manager meminta login
     GitHub di browser dan belum diselesaikan. Setelah login: git push -u origin
     feat/tv-bridge-schedule-integration, lalu buat PR dari branch itu.
  2. VIP-4 dalam keadaan MATI (standby dalam). Saat standby dalam NIC TV mati: ping/ARP hilang dan
     paket WoL TIDAK membangunkannya. Perlu diperiksa di TV: setelan jaringan "jaga koneksi saat
     standby"/Wake-on-LAN. Selama itu belum beres, jangan mengandalkan peringatan otomatis atau
     auto-nyala untuk TV yang sudah lama tidur — harus dinyalakan dengan remote.
  3. Server POS perlu dimuat ulang sekali untuk memakai dua penyesuaian terakhir: batas waktu
     klien bridge 8 -> 12 detik, dan teks pesan untuk aksi notify.
  4. tv_control_logs ROOM-004 masih menyimpan 16 baris catatan uji hari ini (kasir "Uji TV Bridge"/
     "Uji Integrasi TV"). Bukan data keuangan; menunggu keputusan pemilik apakah ikut dibersihkan.
  5. Pengaman masa uji masih aktif: server POS punya TV_BRIDGE_ROOM_ALLOWLIST=ROOM-004 dan bridge
     punya TV_COMMAND_ALLOW_LAN_FALLBACK=true. Kosongkan allowlist saat siap semua ruangan, dan
     matikan fallback setelah halaman kasir dimuat ulang (frontend sudah mengirim token).
  6. Pemasangan ke TV lain: APK overlay + izin + sekali uji per TV (pekerjaan per kunjungan),
     sekaligus memastikan setelan jaringan standby seperti butir 2.

## 3. Cara mengembalikan (rollback) bila perlu

  - Bridge: cd C:\karaoke-tv-bridge && git checkout 6e45eac -- server.js src config  (atau salin dari
    cadangan-20260923\), lalu restart lewat start-tv-bridge.cmd.
  - POS: cd C:\HappySong\happy-song-local && git checkout 6ee074a -- js server/src
    (branch kerja feat/tv-bridge-schedule-integration menyimpan semua perubahan kita).
  - APK di TV: adb -s 192.168.1.104:5555 uninstall com.happysong.tvnotify (terbukti bersih).
  - Token: kosongkan API_TOKEN di C:\karaoke-tv-bridge\.env lalu restart bridge.
