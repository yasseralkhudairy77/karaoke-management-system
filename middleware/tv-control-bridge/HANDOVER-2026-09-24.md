# HANDOVER — status pekerjaan TV bridge (diperbarui 2026-09-23 malam)

Dokumen ini yang dibaca lebih dulu sebelum menyentuh apa pun. Bagian 1 = sudah beres, bagian 2 = masih
menunggu, bagian 3 = cara mengembalikan.

## 1. Yang sudah selesai

  A. Overlay di layar TV (jalur C) — TERBUKTI
     APK com.happysong.tvnotify terpasang di VIP-4 (izin overlay aktif), proyek di
     C:\karaoke-tv-bridge\tv-notify-overlay. Uji T1-T7 lolos. Bukti: HASIL-UJI-OVERLAY-TV.md.
     Sejak 2026-09-23 malam kode proyek ini juga tercermin di repo POS (lihat bagian 2 butir 7).

  B. Bridge memegang jadwal peringatan — TERBUKTI
     Peringatan T-15/T-5 di layar TV + peniduran TV di T-0 berikut tenggang, retry, catatan kejadian
     (logs/tv-events.jsonl), jadwal tahan restart (data/jadwal-ruangan.json), token bersama hidup.
     Bridge: C:\karaoke-tv-bridge (repo git sendiri, commit terakhir fbf6ebe).

  C. Integrasi ke POS — TERBUKTI LEWAT API POS
     - server/src/services/tvBridgeService.js (baru): klien bridge + jadwal + penyapu + audit.
     - routes/api.js: setelah aksi sesi berhasil, jadwal bridge disesuaikan dari tabel rooms.
     - server.js: penyapu saat server hidup, status bridge di /sync/status, /tv-bridge-config.js.
     - js/config.js + js/app.js + index.html: alamat bridge 192.168.1.3:3030, token dari server
       (tidak disimpan di repo karena repo PUBLIK), batas waktu 12 detik.
     Branch: feat/tv-bridge-schedule-integration, SUDAH terkirim ke GitHub (HEAD f35f074).

  D. Database bersih dari data uji — SELESAI
     - Dua transaksi uji (TRX-1790157321548 Rp32.000 dan TRX-1790155003199 Rp28.000) beserta 2 sesi,
       2 segmen sesi, dan 4 baris outbox sudah DIHAPUS. Pembanding: 519 transaksi lain identik
       sebelum-sesudah (sidik md5 59caa7b0...), total nilai turun tepat Rp60.000.
     - 16 baris tv_control_logs ROOM-004 juga sudah DIHAPUS (termasuk 3 baris dari penyapu otomatis yang
       kasirnya "Sistem (penyapu)", semuanya di dalam jendela uji 09:10-09:57). Tabel tv_control_logs
       sekarang kosong. Cadangan: cadangan-20260923\hapus-transaksi-uji-20260923.json dan
       cadangan-20260923\hapus-tv-logs-uji-20260923.json.

## 2. Yang masih menunggu

  1. Server POS belum dimuat ulang sejak 16:23 — padahal server/src/controllers/tvController.js dan
     server/.env diubah 16:27 (batas waktu 12 detik + teks pesan notify belum terpakai). Jalankan
     npm start sekali. Semua perubahan sudah masuk git, jadi restart aman.
     KEPUTUSAN PEMILIK 2026-09-23 malam: restart DITUNDA, dikerjakan besok bersama lanjutan urusan
     menyalakan TV. Jadi jangan menganggap servernya sudah diperbarui — dan jangan me-restart tanpa
     memberi tahu, karena kasir bisa sedang memakai POS.
  2. Jaringan bergeser karena provider internet diganti: router/pool DHCP baru membuat sewa alamat berubah.
     Hasil bandingkan config/rooms.json dengan ARP (2026-09-23 malam):
       VIP-6      config 192.168.1.10  -> sekarang dipegang perangkat lain (MAC 64:6b:e7:2d:59:a2)
       Executive  config 192.168.1.12  -> sekarang dipegang perangkat lain (MAC b8:8f:27:ed:a3:39)
       VIP-2      config 192.168.1.6   -> tidak menjawab ping
       VIP-1, VIP-3, VIP-4, VIP-5, VIP-7, VIP-8 -> MAC masih cocok
     Pemilik akan memeriksa daftar klien DHCP di router sendiri. Perbaikan permanen: reservasi DHCP ke MAC
     NIC kabel TV di blok alamat di luar kolam DHCP. Selama itu belum, config akan terus bocor angkanya.
  3. ADB baru tersambung ke VIP-4 (192.168.1.104). Tujuh TV lain hidup di jaringan tapi port 5555 tertutup:
     ADB belum diaktifkan/diotorisasi. APK overlay hanya terpasang di VIP-4. Jadi rollout ke seluruh ruangan
     = kunjungan per TV (aktifkan ADB + otorisasi + pasang APK + izin overlay + sekali uji), sekaligus
     memastikan setelan jaringan standby.
  4. Pengaman masa uji masih aktif: server POS punya TV_BRIDGE_ROOM_ALLOWLIST=ROOM-004 dan bridge punya
     TV_COMMAND_ALLOW_LAN_FALLBACK=true. Kosongkan allowlist setelah butir 3 selesai, dan matikan fallback
     setelah halaman kasir dimuat ulang (frontend sudah mengirim token).
  5. Catatan jaringan lama (WoL/standby dalam) tidak lagi berlaku untuk VIP-4 sekarang: TV-nya hidup
     (ping 2-9 ms, ARP ada, port 5555 terbuka, ADB state "device", APK + izin overlay masih terpasang,
     layar sedang screensaver DreamActivity). Tetap perlu dicek per TV saat kunjungan.
  6. Temuan sampingan yang belum disentuh: 13 sesi berstatus "starting" tertinggal sejak 12-14 Agustus 2026
     (ROOM-001, 005, 006, 008, 009) sehingga ruangan-ruangan itu masih tampak tidak tersedia di POS walau
     kosong. Baris transaksinya perlu ditelusuri lebih dulu sebelum dibereskan.
  7. Cermin kode bridge di repo POS: middleware/tv-control-bridge sekarang berisi kode yang sama dengan folder
     kerja, ditambah proyek APK overlay dan skrip penjaga tools/tvbridge-sync.ps1. Setelah mengubah kode di
     folder kerja, jalankan skrip itu (-Mode periksa lalu -Mode sync) dan commit di repo POS. Perincian:
     middleware/tv-control-bridge/tools/CATATAN-SINKRONISASI.md.

## 3. Cara mengembalikan (rollback) bila perlu

  - Bridge: cd C:\karaoke-tv-bridge && git checkout 6e45eac -- server.js src config  (atau salin dari
    cadangan-20260923\), lalu restart lewat start-tv-bridge.cmd.
  - POS: cd C:\HappySong\happy-song-local && git checkout 6ee074a -- js server/src
    (branch kerja feat/tv-bridge-schedule-integration menyimpan semua perubahan kita).
  - Cermin kode bridge di repo: git checkout ec9c8b5 -- middleware/tv-control-bridge
    (kembali ke salinan lama 31 Juli; hanya perlu bila ingin membatalkan bagian 2 butir 7).
  - APK di TV: adb -s <ip>:5555 uninstall com.happysong.tvnotify (terbukti bersih).
  - Token: kosongkan API_TOKEN di C:\karaoke-tv-bridge\.env lalu restart bridge.